import AdminControlPanel from "@/components/AdminControlPanel";

// Auth + Layout + AdminNav are provided by the parent admin layout.tsx.
export default function AdminSettingsPage() {
  return <AdminControlPanel />;
}
